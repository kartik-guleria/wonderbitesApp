import {StyleSheet} from 'react-native';
import {
  COLORS,
  FONTS,
  SIZES,
  ComIcons,
  DelIcons,
  NavIcons,
} from '../../../assets/index';

const styles = StyleSheet.create({
  image: {
    height: 160,
    width: 200,
    marginLeft: -20,
    marginTop: 60,
    alignSelf: 'center',
    resizeMode: 'contain',
  },
});
export default styles;
