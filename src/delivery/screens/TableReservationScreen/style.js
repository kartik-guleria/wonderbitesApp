import {StyleSheet} from 'react-native';
import {
  COLORS,
  FONTS,
  SIZES,
  ComIcons,
  DelIcons,
  NavIcons,
} from '../../../assets/index';

const styles = StyleSheet.create({
  image: {
    height: 140,
    width: 250,
    marginLeft: -20,
    marginTop: 50,
    alignSelf: 'center',
    resizeMode: 'contain',
  },
});
export default styles;
